---
import type { SvgIconProps } from '@libs/icon'

type Props = SvgIconProps

const { class: className, height, width } = Astro.props
---

<img
  src="https://raw.githubusercontent.com/LaviniaStiliadou/bootstrap/main/images/logo.png"
  alt="Bootstrap"
  class={className}
  height={height}
  width={width}
/>
