---
title: Glossary
description: Learn about the key concepts and tools used throughout the platform.
---------------------------------------------------------------------------------

This glossary explains some of the key concepts, models, and reusable components used throughout the platform.

### Block

A **Block** is a reusable building element within a quantum low-code model. Blocks encapsulate a specific operation and can be combined with other blocks to create more complex models.

### Low-Code Model

A **Low-Code Model** is a visual representation of an application, process, or system that can be created with minimal hand-written code. Users can build and configure models using graphical elements, properties, and predefined components.

### Placeholder

### Qonstruct

**Qonstruct** is a visual modeling and execution environment for quantum applications. It enables users to build quantum applications using graphical models rather than implementing every component manually.

### Quantum Circuit

A **Quantum Circuit** is a representation of a quantum computation. It describes how quantum bits (qubits) are initialized, manipulated through quantum gates, and measured to produce results.

### Templates

**Templates** are ready-to-use models. They can be customized to match specific requirements, reducing the time and effort needed to build a model from scratch.

### Workflow

**Workflow** refers to a workflow modeled using **Business Process Model and Notation (BPMN)**. BPMN provides a standardized graphical notation for describing business processes, including tasks, events, decisions, and the flow between them.
